# Project: lc_practice

## What this is
LeetCode practice repo. Patterns and solutions, organized under `pattern-atlas/`.

## How to run tests
- Run: `pytest` from the project root (adjust if the project uses a different runner).
- If there are no tests for a change, say so explicitly rather than claiming success.

## Conventions
- Follow the existing file/folder structure under pattern-atlas/.
- Match the style of surrounding code.

## Reminders (see global safe-coding rules)
- Plan → approve → execute. Show `git diff` + test output when done.
- I handle all git writes.
